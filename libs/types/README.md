# @contact-app/types

Shared TypeScript types and Zod schemas for the contact app.

## Exports

- `Contact` — Full contact record (includes id, timestamps)
- `ContactInput` — Contact creation/update input (excludes id, timestamps)
- `contactSchema` — Zod schema for runtime validation
